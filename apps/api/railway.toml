[build]
builder = "NIXPACKS"
buildCommand = "pnpm install && pnpm build --filter @shina/api"

[deploy]
startCommand = "pnpm start --filter @shina/api"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"

[env]
NODE_ENV = "production"
PORT = "3000"
