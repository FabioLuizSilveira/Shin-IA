# Mobile API Gap Analysis

Data: 2026-08-15 · Baseado em [MOBILE_API_AUDIT.md](MOBILE_API_AUDIT.md) e
[MOBILE_SECURITY_REVIEW.md](MOBILE_SECURITY_REVIEW.md).

**Status update (Wave 3)**: esta tabela é o snapshot original da auditoria — mantida como registro
histórico, não editada linha a linha. Gaps fechados desde então: MOB-001/MOB-002 (Wave 1),
MOB-004 (Wave 3 Phase B — `GET /api/customer-contracts/{id}/documents/{documentId}/url`, caminho
real difere do proposto na coluna acima). Ver `mobile-openapi.yaml` para o estado atual e correto de
cada endpoint (`x-shina-status`), não esta tabela.

Prioridade: **P0** = necessário para autenticar/inicializar o app · **P1** = necessário para o loop
funcional principal · **P2** = importante mas não bloqueante para MVP · **P3** = nice-to-have.

| ID      | Domain           | Capability                                                                                                                                           | Existing Core Support                                                                                        | Existing Endpoint                              | Required Endpoint                                                                                                                                   | Priority                                 | Security Impact                                                                                                                       | Recommended Implementation                                                                                                                      |
| ------- | ---------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ | ---------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| MOB-001 | Bootstrap        | Uma chamada que inicializa o app (user/tenant/branding/roles/permissions/entitlements/features)                                                      | Parcial — `jwt-claims.ts` decodifica quase tudo, mas nada agrega isso num payload único                      | Nenhum                                         | `GET /mobile/bootstrap`                                                                                                                             | **P0**                                   | Alto se malfeito (evitar vazar permissões de outro tenant) — mas reduz superfície comparado a N chamadas                              | BFF endpoint (ver ADR) agregando claims decodificadas + query de branding/entitlements, nunca decidindo autorização sozinho                     |
| MOB-002 | Auth             | Identificar `userType` (tenant_user / customer / operator) no bootstrap                                                                              | Existe indiretamente — `rental_customers`/`operators` não têm claim de tenant no JWT, só `user_profiles` tem | Nenhum                                         | Campo `userType` resolvido no bootstrap consultando as 3 tabelas de identidade em ordem                                                             | **P0**                                   | Médio — decisão de qual tabela consultar primeiro importa (evitar confusão entre um staff que também é cliente, se esse caso existir) | Resolver server-side, nunca aceitar `userType` do cliente                                                                                       |
| MOB-003 | Tracking         | Histórico de posição por recurso/dispositivo                                                                                                         | `resource_locations` existe, append-only, com índice `(resource_id, recorded_at)`                            | Só última posição (`/api/resources/locations`) | `GET /mobile/tracking/{resourceId}/history?since=...`                                                                                               | **P2**                                   | Baixo (leitura, já tenant-scoped)                                                                                                     | Nova rota TS, paginada, reaproveitando `resource_locations`                                                                                     |
| MOB-004 | Documents        | Download de documento de contrato pelo lado do CLIENTE                                                                                               | Bucket `contract-documents` privado existe, signed URL já implementado do lado staff                         | `GET /api/contracts/{id}/documents` (só staff) | `GET /mobile/customer-contracts/{id}/documents/{documentId}/url`                                                                                    | **P1**                                   | Médio — precisa validar posse antes de gerar signed URL                                                                               | Espelhar a lógica de auth manual já usada em `customer-contracts/{id}/documents` (cadeia `rental_customers`→`rental_customer_organizations`)    |
| MOB-005 | Contracts        | PDF/evidência de aceite para download                                                                                                                | Snapshot imutável existe (`tenant_contract_snapshots.rendered_content`), sem geração de PDF                  | Nenhum                                         | `GET /mobile/customer-contracts/{id}/evidence.pdf` (ou retornar o texto renderizado + deixar o client formatar)                                     | **P2**                                   | Baixo                                                                                                                                 | Reaproveitar `rendered_content` já armazenado; gerar PDF client-side ou server-side simples, sem nova lib pesada se possível                    |
| MOB-006 | Notifications    | Targeting por usuário (não só broadcast por tenant)                                                                                                  | Coluna `person_id` existe no schema `notifications`, não usada por `createNotification()`                    | `GET/PATCH /api/notifications` (broadcast)     | Nenhuma mudança de rota — mudança na função `createNotification()` para popular `person_id` quando aplicável                                        | **P2**                                   | Baixo                                                                                                                                 | Fora do escopo desta auditoria (implementação), documentar decisão                                                                              |
| MOB-007 | Notifications    | Registro de push token (device)                                                                                                                      | **Nenhum** — zero tabelas/rotas em todo o repo                                                               | Nenhum                                         | Nova tabela `device_push_tokens` (`user_id`, `token`, `platform`, `created_at`) + `POST /mobile/devices/register`, `DELETE /mobile/devices/{token}` | **P1**                                   | Baixo/médio — token deve ser vinculado ao usuário autenticado, nunca aceito para outro `user_id`                                      | Nova tabela + rotas; nenhum provider de push implementado nesta fase (fora de escopo)                                                           |
| MOB-008 | Operators        | `operator_assignments` sem rota própria                                                                                                              | Tabela existe (Fase I do contract engine), sem API                                                           | Nenhuma                                        | `GET /api/operators/{id}/assignments`                                                                                                               | **P2**                                   | Baixo                                                                                                                                 | Rota TS simples de leitura                                                                                                                      |
| MOB-009 | IAM              | `hasTenantPermission()` só cobre 6 rotas — qualquer rota nova para mobile herdaria essa inconsistência se copiada do padrão majoritário              | Catálogo completo existe (`tenant_permissions`/`tenant_role_permissions`)                                    | 6 rotas                                        | Nenhuma rota nova — **decisão de padrão**: toda rota nova do BFF mobile deve chamar `hasTenantPermission()`, nunca só `isTenantAdmin()`             | **P0** (decisão de padrão, não uma rota) | Alto — é o principal risco de regressão de autorização ao construir o BFF                                                             | Registrar como regra de implementação obrigatória no plano (Wave 1)                                                                             |
| MOB-010 | Tenant isolation | Ausência de RLS-backstop nas rotas de staff (~80 rotas hoje, e o BFF mobile herdaria o mesmo padrão se não for cuidadoso)                            | `scopedSelect`/`scopedUpdate`/etc. já existem como padrão recomendado                                        | Parcial                                        | Nenhuma rota nova — regra: toda rota nova do BFF mobile usa os helpers `scoped*`, nunca `.eq()` manual solto                                        | **P0** (decisão de padrão)               | Alto                                                                                                                                  | Registrar como regra obrigatória, Wave 1                                                                                                        |
| MOB-011 | Tracking         | HMAC de webhook de ingestão de GPS não é obrigatório                                                                                                 | Verificação existe mas é condicional (`if header present`)                                                   | `/api/webhooks/fleet-location/{token}`         | Tornar `X-Signature` obrigatório                                                                                                                    | **P2**                                   | Médio                                                                                                                                 | Fora do escopo mobile direto (é ingestão de dispositivo), mas relevante à confiabilidade dos dados que o mobile vai ler                         |
| MOB-012 | Auth             | App mobile permite auto-cadastro (magic-link self-service) sem gate de convite, contrariando a intenção original de "staff-invite"                   | `rental_customers` sem policy de insert visível — criação provavelmente via convite server-side hoje         | Login screen atual permite qualquer e-mail     | Decisão de produto: manter self-service ou reforçar gate                                                                                            | **P1** (decisão de produto/segurança)    | Alto (potencial)                                                                                                                      | Confirmar configuração do Supabase Auth (auto-confirm) e decidir explicitamente — não é um "conserto" técnico isolado, é uma decisão de produto |
| MOB-013 | Billing          | Rotas `/api/commercial/plans` e `/api/commercial/contract` são públicas — confirmar que isso é intencional para o app mobile também (pré-onboarding) | Confirmado no código, propositalmente sem tenant scope                                                       | Implementado                                   | N/A — já cobre o caso de uso                                                                                                                        | **P3** (só documentar)                   | Baixo (leitura de catálogo público por design)                                                                                        | Nenhuma ação, só confirmar no OpenAPI que é `security: []`                                                                                      |
| MOB-014 | Rate limiting    | Limiter in-memory não é distribuído — relevante se o BFF mobile for para Railway (múltiplas instâncias)                                              | Documentado como limitação conhecida                                                                         | `apps/web/src/lib/rate-limit.ts`               | Migrar para Upstash Redis (ou equivalente) antes de escalar o BFF mobile horizontalmente                                                            | **P2**                                   | Médio                                                                                                                                 | Fora do escopo desta auditoria (implementação futura)                                                                                           |
| MOB-015 | Dashboard        | `/api/tenant-metrics`/`/api/tenant-reports` não validados para payload mobile-friendly (podem ser pesados demais / formatados para web)              | Implementado para web                                                                                        | `/api/tenant-metrics`, `/api/tenant-reports`   | Confirmar payload size/shape antes de reusar 1:1 no mobile; possivelmente precisa de versão resumida                                                | **P2**                                   | Baixo                                                                                                                                 | Avaliar na Wave 2                                                                                                                               |
